%h2= t("passwords.new.title")
%p= t("passwords.new.blurb")
- semantic_form_for :password, :url => passwords_path do |form|
  - form.inputs do
    = form.input :email, :label => t("passwords.new.email_label")
  - form.buttons do
    = form.commit_button t("passwords.new.reset_button")
